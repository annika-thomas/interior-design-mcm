# Optional. Without a key the app still runs end to end using the built-in
# heuristic analyzer + MCM rule engine; with a key, photo analysis and
# suggestions get much sharper.
ANTHROPIC_API_KEY=

# Model used for photo analysis and design suggestions.
MCM_MODEL=claude-opus-5

PORT=8787
DATA_DIR=./data
